# pnpm workspace: web, mobile, apis at root; shared types/schemas in packages.
# Enables: pnpm install (one install), pnpm --filter <name> <script>
packages:
  - "web"
  - "mobile"
  - "apis"
  - "packages/*"
