# pnpm workspace: web, app, apis at root; shared types/schemas in packages.
# Enables: pnpm install (one install), pnpm --filter <name> <script>
packages:
  - "web"
  - "app"
  - "apis"
  - "packages/*"
